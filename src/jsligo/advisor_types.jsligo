type advisorAlgo = ((int) => bool);

type advisorStorage = {
    indiceAddress : address,
    algorithm : advisorAlgo,
    result : bool
};

type advisorEntrypoints = ["ReceiveValue", int] | ["RequestValue", unit] | ["ChangeAlgorithm", advisorAlgo];

type advisorFullReturn =  [list<operation>, advisorStorage];